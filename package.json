{
  "name": "smart-home-app-sample",
  "version": "0.0.1",
  "description": "Mock implementation of device manufacturer platform to facilitate development of sample device and adapter.",
  "main": "build/index.js",
  "private": true,
  "scripts": {
    "build": "npm run compile && cp src/*.json build/",
    "deploy": "npm run build && gcloud app deploy && gcloud app logs tail -s default",
    "start": "node build/index.js",
    "start:local": "node build/index.js isLocal",
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix"
  },
  "author": "Google Inc.",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=10.0.0"
  },
  "dependencies": {
    "actions-on-google": "^2.12.0",
    "body-parser": "^1.18.3",
    "cors": "^2.8.4",
    "express": "^4.16.0",
    "firebase-admin": "~8.10.0",
    "morgan": "^1.9.0",
    "ngrok": "^3.2.5"
  },
  "devDependencies": {
    "typescript": "^4.0.3",
    "gts": "^3.0.2",
    "@types/cors": "^2.8.4",
    "@types/morgan": "^1.7.35"
  }
}
